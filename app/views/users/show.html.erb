<section class="notes-list">
  <div class="notes-list-header"><%= @user.username %></div>
  <ul class="notes">
    <% @user.notes.each do |note| %>
      <li>
        <a href="<%= user_note_url(@user, note) %>">
          <%= render partial: "notes/preview", locals: { note: note } %>
        </a>
      </li>
    <% end %>
  </ul>
</section>